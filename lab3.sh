#!/bin/bash
#Să se scrie un script shell care îmi listează conținutul directorului curent. Se vor face trei afișări: una sortată după numele fișierelor, alta după timpul ultimului acces la ele și cea de-a treia, după dimensiunea în bytes a acestora.
ls -l;
ls -ltu;
ls -lS;
ls -lSr;
